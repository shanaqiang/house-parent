<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shana.house.mapper.ManagerNotesMapper">
    <resultMap id="BaseResultMap" type="managerNotes">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Mon Nov 18 20:16:27 CST 2019.
        -->
        <id column="nid" jdbcType="INTEGER" property="nid" />
        <result column="title" jdbcType="VARCHAR" property="title" />
        <result column="status" jdbcType="INTEGER" property="status" />
        <result column="mid" jdbcType="INTEGER" property="mid" />
    </resultMap>

    <select id="selectAllByMid" parameterType="int" resultType="managerNotes">
        select * from t_managernotes where mid = #{id}
    </select>

    <insert id="insert" parameterType="managerNotes">
        insert into t_managernotes values (null,#{title},#{status},#{mid})
    </insert>

    <delete id="delete" parameterType="int">
        delete from t_managernotes where nid = #{id}
    </delete>

    <update id="update" parameterType="managerNotes">
        update t_managernotes set status = #{status} where nid = #{nid}
    </update>

</mapper>